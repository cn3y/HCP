apiVersion: v1
kind: Service
metadata:
  name: golf-handicap-backend
  labels:
    app: golf-handicap-backend
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: golf-handicap-backend
